var greet = function (name) {
    if (name[0].toLowerCase() === 'j') {
        console.log("Goodbye " + name + "!");
    } else {
        console.log("Hello " + name + "!");
    }
};

greet("John");

greet("jun");

greet("Spencer");

